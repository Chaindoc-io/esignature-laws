source "https://rubygems.org"

# Local preview only — production build is done by GitHub Pages CI.
# We use plain Jekyll 4 (not the github-pages metagem) so this runs on
# any modern Ruby. The plugin list mirrors _config.yml.
gem "jekyll", "~> 4.3"

group :jekyll_plugins do
  gem "jekyll-seo-tag"
  gem "jekyll-feed"
end

gem "webrick", "~> 1.8"
gem "csv"
gem "base64"
gem "bigdecimal"
